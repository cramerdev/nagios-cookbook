# Hostgroup definitions
# Automatically generated by Chef

define hostgroup {
  hostgroup_name all
  alias All
  members *
}

<% (@roles + @environments).each do |r| -%>
define hostgroup {
  hostgroup_name <%= r.name %>
  alias <%= r.description %>
}
<% end -%>
